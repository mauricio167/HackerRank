#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
   int x,n,i;
    int sum=0;
    scanf("%d", &n);
    for(i=0;i<5;i++){
    x=n%10;
    n=n/10;
    sum=sum+x;

}
printf("%d",sum);
    return 0;
}
